# Metro (React Native bundler) cannot follow pnpm's default symlinked node_modules, so the
# customer Expo app needs a flat layout. `hoisted` makes pnpm install like npm/yarn-classic.
node-linker=hoisted
