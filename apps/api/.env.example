# API (server-only secrets — NEVER prefixed VITE_/EXPO_PUBLIC_, never bundled into a client).
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/hyper
AUTH_TOKEN_SECRET=change-me-in-prod
PAYMOB_HMAC_SECRET=change-me-paymob-hmac
# PAYMOB_API_KEY=            # required for live online payments (Paymob)
PORT=3000
NODE_ENV=development
